# Coulsy Joinery — Site Audit: What To Fix, In Order

**Bottom line:** the site is not broken in the way you'd expect. The contact form *does* work. The 277 "server errors" in Search Console are stale ghosts from a past outage — there is no 5xx anywhere on the live site today. What *is* costing you is: a dead link on all 1,035 pages, a homepage nobody can call you from, ~27% of your search traffic landing on duplicate URLs Google is quietly killing off, and a large chunk of your best-ranking pages selling work you can't or won't do.

Nothing below requires an engineer except the fencing page and the URL redirects.

---

## 1. BLOCKERS — fix this week

### 1.1 The footer links to coulsycontractsolutions.co.uk on every single page
**What:** `src/components/Footer.astro:121-127` — the "Property Maintenance" link points at the domain you gave up. It's on 1,035 of 1,035 built pages (verified against the build output). It also has `target="_blank"` with no `rel="noopener"`, so if a squatter re-registers it, they get a handle on your visitors' browser tab from every page.
**Cost:** every page vouches for a domain you don't control, and passes link equity to it.
**Do:** change the `href` to `/joinery-services/building-maintenance`, relabel the anchor "Building Maintenance", delete `target="_blank"`. That turns a sitewide leak into 1,035 internal links into a page you actually want ranked.
**Effort:** 2 minutes. **Ship it today, independent of everything else.**

### 1.2 Your homepage has no way to phone you
**What:** `dist/index.html` between `<main>` and `<footer>` contains **zero** `tel:` links, zero forms, zero mailto. The phone number is in the navbar inside `hidden lg:flex` (invisible on mobile), inside the collapsed hamburger (two taps), and in the footer (bottom of a very long page). The hero is `h-[80vh]` so it eats the whole first mobile screen, and its button says "Our Services".
**Cost:** the homepage is your single biggest page — 184 clicks / 10,229 impressions. Mobile is 55% of all your clicks (686 of 1,249). Every service page already has a big "Call Now" button; the homepage is the only page that doesn't.
**Do:** `ProminentPhoneButton.astro` already exists and works. Render it in/under the hero in `src/components/MainPage.astro`. Add a fixed-bottom `lg:hidden` sticky call bar. Move the phone out of `hidden lg:flex` in `Navbar.astro:154-169` so it shows in the mobile header.
**Effort:** 30 minutes. Highest ROI change on the site per minute spent.

### 1.3 Form submissions arrive — but the customer thinks it broke
**What:** Good news first: the form is a **Netlify Form** (`ModernContactForm.astro:27-36`, `data-netlify="true"` confirmed in the built HTML). Submissions are being captured. **You are not losing leads.** The `/api/contact` file is orphaned dead code that never runs.
But two bugs make a successful send *look* like a failure:
- `ModernContactForm.astro:291` looks for `getElementById("modern-contact-form")`; the form's id is `contact`. That's always `null`, so the loading spinner never appears and the submit button is never disabled (double-submits possible).
- The form has no `action`, so Netlify bounces the customer to its own generic unbranded "Thank you" page. Your own beautiful success message (lines 160-201) only shows on `?success=true`, which nothing ever sets.
**Do:** (a) change line 291 to `document.querySelector('form[name="contact"]')` — do **not** use `getElementById("contact")`, it collides with a `<section id="contact">` on the FAQ page; (b) add `action="/thank-you"` and create a simple `src/pages/thank-you.astro` restating your phone number and "I'll come back to you within a few hours". That also gives you something to measure.
**ACTION FOR YOU:** log into Netlify → Forms, confirm the `contact` form is listed and that email notifications go to robert@coulsy.co.uk. I can't check that from the code. **Do this before anything else** — 60 seconds.
**Effort:** 1 hour including the thank-you page.

### 1.4 Two broken internal links across ~700 pages
**What:**
- Seven "view my credentials" links point at `/qualifications`, which 404s. The real page is `/about/qualifications`. Files: `joinery-services/carpenter.astro:117` and `:191`, `joiner.astro:197`, `small-build-services.astro:253`, `bespoke-joinery.astro:190`, `general-joinery.astro:124`, `building-maintenance.astro:340`. Because each location page re-renders these, that's **399 dead links across 342 pages**.
- `door-hanging.astro:145` links to `/joinery-services/steel-fire-exit-door-installer` (singular). The real route is plural. Dead link on all 57 door-hanging pages.
**Cost:** the exact click a hesitant customer makes to check you're qualified lands on a 404.
**Do:** find/replace `href="/qualifications"` → `href="/about/qualifications"`. Fix the singular slug.
**Effort:** 10 minutes.

### 1.5 The 277 "Server errors" — nothing to fix, but you must click a button
**What:** I swept the entire live site (2,054 URLs, Googlebot user-agent). Every one returned 200. This is a static site with no server code; it physically cannot throw a 500. The 277 are frozen from a historic outage (indexed pages fell 611 → 406 during Jan–Mar 2026, a period with zero deploys — a hosting/build failure, not a code bug). Coverage is already recovering on its own (406 → 452).
**Do:** In Search Console → Pages → "Server error (5xx)", click **VALIDATE FIX**. Same for "Not found (404)". Nothing clears faster without it. Also turn on Netlify build-failure notifications so a silent 3-month outage never happens again.
**Effort:** 5 minutes. Do not let anyone hunt for a 5xx bug in the code — there isn't one.

### 1.6 Your Google reviews API key is dead
**What:** `dist/api/reviews` is a frozen file containing: `"Google Places API error: REQUEST_DENIED - The provided API key is invalid."` It's served with a 200. Nothing on the site actually reads it (reviews are hardcoded, so visitors see reviews fine) — but it's a public URL serving an error.
**Do:** delete `src/pages/api/reviews.ts` and `src/pages/api/contact.ts`. Both are dead code. Don't try to wire live reviews into a static build — it just makes your deploys depend on Google's API.
**Effort:** 5 minutes.

---

## 2. KEYWORD MOVES — where the money is

### 2.1 Fix the .html duplicate URLs — DO THIS FIRST, it doubles the value of everything else
**What:** Every service page is live at **two** URLs: `/joinery-services/leeds-joiner` and `/joinery-services/leeds-joiner.html`. Both return 200. This is a leftover from an old build config. `public/_redirects` redirects only 7 top-level `.html` paths and **zero** service pages.
**The number:** 463 `.html` URLs in Search Console carry **88,921 impressions and 347 clicks — 27% of your entire click volume**. Examples: `leeds-door-hanging.html` 12 clicks / 3,543 impressions; `wetherby-kitchen-installers.html` 14 clicks; `horsforth-kitchen-installers` is split 1,536 + 1,709 impressions across the two versions. Google is showing the ghost twin more than the real page, and is slowly deprecating it — those 347 clicks will decay to zero.
**Do:** Ship a Netlify Edge Function on `/*` that 301s any HTML page path ending in `.html` to the same path without it. **Scope it to HTML pages only** — do not lowercase or touch `/_astro/` asset paths, or you'll add a redirect hop to every image on every page load. One file, kills all ~1,000 twins.
**Also add to `public/_redirects`:** `/web-development / 301!` (595 impressions currently 404ing) and `/projects/* /joinery-services 301!` (99 impressions).
**Effort:** half a day. **This is the single biggest SEO fix available.** Every title rewrite below is worth roughly double once the signal is consolidated onto one URL per page.

### 2.2 FENCING — 4,075 impressions, zero clicks, and you have no page
**The number:** 14 fencing queries, 4,075 impressions, **0 clicks**. Three towns already rank on **page 2** — "fencing barwick-in-elmet" 989 impr @ pos 14.6; "fencing east keswick" 895 @ 13.8; "fencing boston spa" 460 @ 14.7. Plus decking (444 impr, "decking installers in stamford bridge" already at pos 9.6 — page 1) and gates.
**Why it's absurd:** you rank page 2 off **one image caption**. `general-joinery.astro:217` says "Treated fencing and gates for bin store". That's the only fencing content on the entire site. Fencing isn't even in the general-joinery service list.
**Do:** Create `src/pages/joinery-services/fencing-and-gates.astro` + `[location]-fencing-and-gates.astro`, copying the general-joinery pattern. Generate for **all 57 towns** (do not restrict to 18 — a partial list breaks the "nearby areas" grids and the sitemap, and adds 404s). Move the fencing/decking gallery photos over. Title: `${pageTitle} | Timber Fencing, Gates & Decking Fitted by a Local Joiner`.
**Effort:** 1 day. **Highest-ROI new page on the site.** Fencing, gates and decking are on-site timber work — no workshop needed.

### 2.3 Retitle the door-hanging page to say "Fire Door" — 5 minutes, 6,500 impressions
**The number:** 41 fire-door queries = ~6,750 impressions, 2 clicks. "fire door installation leeds" alone: **1,457 impressions, 0 clicks, position 48.5**.
**Why you're losing:** `door-hanging.astro` **already is** a fire door page — four fire-door gallery photos, a whole "Fire Safety Compliance is Critical" section citing Approved Document B and the RRO 2005, and your FireQual registration. That content is why `leeds-door-hanging.html` already pulls 3,543 impressions on fire-door queries. But the words "fire door" appear nowhere in the **title** (`door-hanging.astro:42` — "Professional Door Hanging Services by a Qualified Joiner") or the badge (`:61`).
**Do:**
- `door-hanging.astro:42` → `` `${pageTitle} | Internal, External & Fire Door Installation` ``
- `door-hanging.astro:61` badge → "Door Hanging & Fire Doors"
This inherits across all 57 town pages automatically.
**Then:** add a "Fire door inspections & maintenance" section to the same page. There's a Harrogate compliance cluster sitting there: "fire door maintenance harrogate" 437 impr, "fire door inspections harrogate" 363, "fire door testing harrogate" 355 — **9 queries, ~1,500 impressions, 0 clicks**. That's recurring-revenue inspection work, all on-site.
**DO NOT** build a separate `fire-door-installation` page tree. It would spawn 57 near-duplicates cannibalising door-hanging — which is already your best cluster (wakefield 17 clicks, pontefract 14, selby 12, leeds 12, harrogate 11).
**Effort:** 5 minutes for the retitle; half a day for the inspection section.

### 2.4 Homepage title and H1
**The number:** homepage is 184 clicks / 10,229 impressions at position 23. The title is `"Coulsy Joinery - Professional Joinery Services in York | Bespoke Kitchens & Garden Offices"` (90 chars — Google cuts at ~60, so the second half is never seen). It leads with your brand name, which draws **11 impressions a year**. Meanwhile "joiners york" is 1,705 impressions and "carpenter york" 1,070.
Worse: the page ships **three `<h1>` tags** (`HeroSlider.astro:54` renders one per slide), and the main one says **"Welcome to Coulsy Joinery & Small Build"** — zero keywords.
**Do:**
- `MainPage.astro:48-49` title → `Joiners in York, Wetherby & Harrogate | Coulsy Joinery`
- `MainPage.astro:51` description → cut to ~150 chars, **drop "heritage restoration" and "bespoke kitchens"**. E.g. *"Time-served joiners covering York, Wetherby, Harrogate and Leeds. Kitchen fitting, door hanging, roof trusses, stud walls, fencing and repairs. Free quote."*
- `HeroSlider.astro:54` — render `<h1>` only for slide 0; make slides 2-3 `<h2>`. **Careful:** `HeroSlider.astro:243` uses `querySelector('[role="banner"]')` to drive autoplay and swipe — if you also remove that role (you should, it's an accessibility error), give the div an `id` and update line 243, or you'll break the slider.
- `MainPage.astro:19` slide 1 headline → "Joiners in York, Wetherby & Harrogate"
**Honest expectation:** at position 23 this is a *ranking* problem, not a title problem. Title tuning moves CTR modestly. The .html fix (2.1) and the mobile call button (1.2) matter more.
**Effort:** 1 hour.

### 2.5 Horsforth kitchens & roof trusses — small wins
- **Horsforth kitchens:** 6 queries at positions 5.9–11.1 ("kitchen fitter horsforth" 383 impr, "best kitchen fitter horsforth" 157, "independent kitchen fitter horsforth" 134). Your title already contains "Fitter" but breaks up the exact phrase. `kitchen-installers.astro:45` → reorder so **"Kitchen Fitter"** appears as an exact pair. Add a reviews link above the fold (people literally search "kitchen fitter horsforth reviews"). The page is split 1,536 + 1,709 impressions across the .html twin — 2.1 fixes that.
- **Roof trusses:** "roof truss installers" 624 impr @ pos 8.36 (5 clicks), "roof trusses leeds" 173 @ 6.5, "cut roof" 110 @ 9.85. The title is already fine. Just apply the .html 301 (762 impr clean vs 853 on the ghost) and put the words "roof trusses" and "cut roof" verbatim in the intro copy.

**Context:** across everything, there are **39 queries sitting at positions 5–15 with 100+ impressions, worth 10,958 impressions and returning 19 clicks**. These are all page-2 results that a title/H1/intro rewrite can push up. The fencing and fire-door clusters above are the two biggest.

---

## 3. STALE CONTENT

### 3.1 Your experience figure contradicts itself three ways — **YOUR DECISION NEEDED**
The site says "35+ years" in 24 places (homepage, about, contact, FAQ), "30+ years" on every single service page and in `pageMeta.ts`, and anchors to "since 1989" (which is 37 years in 2026). One page says **"since 1988"** (`building-maintenance.astro:343`), which matches neither your Craft certificate (1987-89) nor your Advanced one (1990).

Both "35+" and "30+" are technically true — they're floor claims — so nothing is a lie. But a customer moving from your homepage to a service page watches the number drop by five years, which looks careless.

**Decide:** one year, one number. My recommendation: **"Joiner since 1989"** and **"35+ years"** everywhere (understated is safe). Then fix `building-maintenance.astro:343` to the correct certificate date.
**Effort:** 2 hours of find/replace — but **do the heritage/bespoke decision (section 4) first**, because several of those 45 strings sit in copy you're going to delete anyway.

### 3.2 Contact page says "Trading since: July 2013" next to "35+ years in the trade"
`src/pages/contact.astro:278` labels your Coulsy Ltd incorporation date "Trading since:". Same page says "After 35+ years in the trade". Both are true; the label is just the wrong noun.
**Do:** change the label to **"Company registered:"**. Optionally add a "Joiner since: 1989" row.
**Effort:** 1 minute.

### 3.3 "VAT registered: Available on request" — **YOUR DECISION NEEDED**
`contact.astro:293-299` renders the label "VAT registered:" against the value "Available on request". It obviously means *"VAT number: available on request"* — the label has the wrong noun, same as above.
**Do:** relabel to **"VAT number:"**, or better, just print the number (add `VAT: "GB..."` to `src/constants/details.ts`). Six other places on the site state flatly that you're VAT registered.
**Tell me the VAT number and I'll wire it in.**
**Effort:** 5 minutes.

### 3.4 The fire door statistics page — this is your biggest legal exposure
`src/components/FireStats.astro` publishes four figures as fact, with **no source and no date**, under the heading "Recent UK fire statistics": 75% fire door failure rate, 6,500 workplace fires annually, 11-minute response time, and — the problem one — **"35+ Years of fire safety experience"**.

That 35 is provably just your generic since-1989 joinery figure, relabelled. Claiming 35 years of *dedicated fire-safety experience*, on a page selling **fire doors** (a life-safety product), with no evidence, is the single most legally exposed claim on the site. And it ships on **58 URLs** — the door-hanging cluster, which is 21,955 impressions, your most-viewed pages.

The same page also says "30+ years experience" in two other places. So it contradicts itself.

**Do:** replace the fourth tile with something you can actually evidence — **"FireQual Fire Door Inspection certified"** (it's real, it's already on your qualifications page). Either cite the other three stats with a source and year (Home Office fire statistics; FDIS inspection data) or delete the stat grid. One component edit fixes all 58 pages.
**Effort:** 1 hour. **Do it soon.**

### 3.5 Your reviews say "a month ago" — they were written 9 months ago
`Reviews.astro:12-73` hardcodes 10 reviews with **literal relative-time strings** ("a month ago", "a year ago"). Git says they were written 2025-11-05 — eight months ago. So a review displayed as "a month ago" is really ~9 months old, and the "a year ago" ones are pushing two years. They can never update and drift further daily.
The reviews themselves are genuine (real names, real jobs). It's the fake freshness that's the problem — and freshness is exactly what a prospect scrutinises.
**Do:** replace the relative strings with absolute dates rendered as "March 2025", or drop dates entirely. Either ages honestly.
**Effort:** 20 minutes.

### 3.6 Star rating markup on 1,000+ pages that show no reviews
`src/utils/pageMeta.ts:365-371` tells Google every service page has a 5.0 rating from 10 reviews. But those pages render `ReviewsLinkSection` — a text link, **zero reviews shown**. Claiming a rating for reviews that aren't on the page is a Google policy breach and makes the markup ineligible anyway.
**Do:** delete the `aggregateRating` block from `pageMeta.ts:365-371`. **Leave `MainPage.astro:153-158` alone** — the homepage genuinely does render 10 five-star reviews, so its markup is accurate and compliant.
**Effort:** 2 minutes.

---

## 4. STRATEGIC — heritage, bespoke, and the 1,000-page problem

### 4.1 You are efficiently generating exactly the leads you told me you don't want
Heritage is not a stray mention — it's a **top-level nav item** (`Navbar.astro:52-55`), a **homepage service card** (`OurServicesBody.astro:94-101`), the **sitewide default keywords** (`Layout.astro:32`), a **homepage schema Offer** ("Expert restoration and reproduction of period joinery" — `MainPage.astro:125-128`), and — worst — an **auto-generated FAQ answer injected into every town page's structured data**:

> `pageMeta.ts:456`: *"Do you work on period or listed properties in {town}?" → "Yes… sympathetic joinery repairs — sash window restoration, door repairs, timber-frame and heritage work — while respecting the property's character and any conservation requirements."*

That fires on ~360 pages, most of which have nothing to do with heritage. It is the single strongest heritage lead-magnet in the codebase and it's in machine-readable schema.

**And it works.** `leeds-heritage-restoration-joinery` is your **#2 page site-wide** (36 clicks / 1,199 impressions). Live heritage URLs: 29 pages, **69 clicks/year (5.5% of all clicks)**.

Meanwhile **bespoke** promises made-to-order staircases, timber windows, garage doors and handcrafted furniture on 58 URLs (`bespoke-joinery.astro:40, 67, 124, 149, 286`) — plus the galleries on `joiner`, `carpenter`, `general-joinery` and `joinery` all showcase handmade cabinets, oak mirror frames and a handmade garage door. You have no workshop. Those are promises you cannot keep. That cluster earns **~58-79 clicks/year**, and the fabrication *queries* convert at basically 0% (15 staircase queries, 845 impressions, **zero clicks**).

### 4.2 My recommendation: REWRITE IN PLACE. Do not delete.
Deleting 114 URLs throws away real traffic and real ranking authority for nothing — and geo-targeting is the thing that works for you. Instead:

**Kill the promotion (zero traffic cost, do this now):**
- `Navbar.astro:52-55` — remove "Heritage & Restoration". **Put "Door Hanging & Fire Doors" in that slot** — it's your best cluster (158 clicks / 21,955 impressions) and it's currently linked from *nowhere* on the site. It has zero internal links. That's a straight swap of a service you don't want for one you already rank for.
- `OurServicesBody.astro:94-101` — remove the Heritage card (it also says "reproduction of period joinery", which breaks the workshop rule too).
- `MainPage.astro:51, 53, 62, 121-129` — strip heritage from the homepage description, keywords, schema description and Offer.
- `MainPage.astro:99-102` — reword the "Bespoke Joinery" Offer away from "made-to-measure".
- `Layout.astro:32` — strip "heritage restoration" and "bespoke joinery" from the default keywords.
- `pageMeta.ts` — **this is the important one.** Delete the period/listed-property FAQ (`:453-458`); remove "heritage repairs" from the description variants (`:275`, `:282`); drop the terms from keywords (`:289`), `knowsAbout` (`:323`), and the fallback FAQ answer (`:462`).
- `joinery-services.astro:12, 69`; `faq.astro:134-158` (the whole Heritage FAQ block) and `:59-83` (the Bespoke FAQ block — it literally quotes **"4-8 weeks for complex custom furniture"**, a workshop lead-time schedule); `public/site.webmanifest:4`.

**Repoint the pages (keeps the 69 + 79 clicks and the rankings):**
- `heritage-restoration-joinery.astro:43` title → `${pageTitle} | Sash Window Repairs & Timber Repairs for Older Homes`; `:64` hero → describe sash-window restoration and door/timber repair. Sash restoration is work you *will* take. Same URL, same rankings, different phone calls.
- `bespoke-joinery.astro` → reframe as **fitted joinery, installed on site**: alcove units, panelling, shelving, window seats, fitted storage. Title → `${pageTitle} | Made-to-Measure Fitted Joinery, Installed On Site`. Delete "custom staircases", "handcrafted furniture", "garage doors", "traditional timber windows", and the listed-buildings paragraph (`:153-157`).
- **Swap the fabrication photos out of every gallery** — the handmade vanity cabinet, oak mirror frame, picture frame and garage door appear on `bespoke-joinery`, `general-joinery`, `joiner`, `carpenter` and `joinery`. They are the strongest visual promise of workshop fabrication on the site. Replace with fitted/installed work.
- Do the same sweep on `joiner.astro` (`:36, :64, :120-124, :133-135, :283`) and `joinery.astro` (`:126-127` "supply only", `:140` "handmade pieces"). `joiner.astro` matters most — it's the shared template behind 57 town pages and 147 clicks.

**Effort:** 2-3 days. This is the biggest single job in the audit and it's the one that actually changes who phones you.

### 4.3 The 1,000-page index problem — **YOUR DECISION NEEDED**
You have **1,027 service pages** (18 services × 56 towns). Google has indexed **452 of 1,481 known URLs — 30%** — and 604 are sitting in "crawled/discovered, not indexed".

I measured why: `leeds-joiner` and `otley-joiner` share **96.6%** of their vocabulary. Exactly five words differ, and they're all just the nearby-town list. Google sees a doorway template and refuses to index.

**The geo axis is NOT the problem.** Only 5 of 56 towns earned zero clicks. Leeds pulls 132, Wetherby 98, Pocklington 66, Otley 56, York 53. Your geo strategy works.

**The SERVICE axis is the problem.** You have four separate 57-page templates fighting over the same intent — `joiner`, `carpenter`, `joinery`, `general-joinery`. And `steel-fire-exit-doors-installers` gets **16 clicks on 23,849 impressions** (0.067% CTR — Google is ranking it for queries that aren't your business; the Ossett cluster alone is 16,249 impressions and **zero clicks in a year**, almost certainly non-UK bot noise).

**My recommendation — prune services, keep all 56 towns:**
1. Merge `carpenter` + `joinery` + `general-joinery` → **`joiner`** (147 clicks, converts in 29 of 56 towns — widest footprint). 301 the rest into it.
2. Merge `accessible-kitchen-installers` → `kitchen-installers`; `truss-roof-installers` → `traditional-cut-roofs`; `garden-rooms` → **`garden-offices`** (offices outperform rooms: 25 clicks/11,550 impr vs 21/6,084 — the earlier draft had this backwards); `building-maintenance` → `small-build-services`.
3. Keep and add: `joiner`, `door-hanging`, `kitchen-installers`, `stud-wall-partitioning`, `traditional-cut-roofs`, `garden-offices`, `joinery-subcontractors`, `small-build-services`, **`fencing-and-gates`** (new).
4. Result: ~9 services × 56 towns ≈ **510 pages, down from 1,027**. Every merge keeps its equity via a 301.

**But the prune only works if you also fix the thinness.** Each surviving town page needs at least one genuinely local paragraph — a real job you did there, the postcode area, callout terms. If you can't write that for 500 pages, cut the town list to the ~30 that actually convert and write real content for those.

**This is your call.** It's a real business decision with 190 clicks/year attached (of which ~150 are leads you've said you don't want). Tell me which way and I'll execute it.

---

## 5. TECH DEBT — what's worth it

**Worth doing (quick wins):**
- **`astro.config.mjs:68-80` — delete the whole `vite` block.** `minify: false` is in there. Everyone assumed it was a workaround for the old "CSS breaking in production" bug. Git proves it isn't — it was added in the *same* commit that caused that churn, and all four actual fixes came later and never touched it. I tested removal: build is clean, CSS identical. **Saves 6KB gzipped and 2 HTTP requests on every uncached visit**, mostly render-blocking CSS. *(Smoke-test the navbar toggle and smooth-scroll after — minification inlines those two scripts.)* — 10 minutes.
- **Delete `src/pages/sitemap-index.xml.ts`.** 153 lines of dead code, silently overwritten on every build by the real sitemap plugin. And it's *wrong* — it omits `door-hanging` entirely, your best service. If anyone ever "fixes" the collision by trusting it, 57 of your best pages drop out of the sitemap. — 2 minutes.
- **Delete the dead files:** `ViewTransitions.astro`, `InteractiveTestimonials.astro` (it has fabricated vanity stats — "500 happy clients, 1,000 projects, 98% satisfaction" — that you could never evidence if asked), `LocationSpecificContent.astro`, `ServicePageHero.astro`, `NearbyAreasList.astro`, `src/pages/robots.txt.ts`, and `npm uninstall astro-robots-txt`. — 15 minutes.
- **Delete 30 unreferenced images (20.8 MB)** — including two 6 MB copies of `rooftop.jpg`. Repo hygiene, not a speed fix. — 20 minutes.
- **Delete the 48 dead component imports** across 17 service pages — they're 100% of the 56 warnings in your build, which means a genuinely new warning would go unnoticed. — 30 minutes.
- **Delete the stale docs** in the repo root — `PERFORMANCE_IMPROVEMENTS.md` describes a caching service worker you don't have, and five others actively recommend the heritage/bespoke strategy you've rejected. **Leave them and the next person to touch this site will re-propose exactly what you turned down.** — 5 minutes.

**Safely ignorable:**
- **Astro 6/7 upgrade: NO. Don't.** It's not a version bump — it's a bundled Node 22 + Tailwind 4 migration, because `@astrojs/tailwind` is frozen and doesn't support Astro 6 or Tailwind 4. That's a week of work for zero customer benefit. Astro 5 is maintained. Just take the safe minors: `npm i astro@5.18.2 @astrojs/sitemap@3.7.3 @astrojs/check@0.9.9 sass@1.101.0`.
- The `tel:` links have a space in them (`tel:07544 030486` — `details.ts:2`). Non-standard, but every mainstream browser handles it. Tidy it when you're next in that file; don't make a job of it.
- Sitemap `lastmod` stamps build-time on all 1,031 URLs (a useless signal). Delete the line in `astro.config.mjs:51`. Nice-to-have.

---

## THE FIVE DECISIONS I NEED FROM YOU

1. **Netlify Forms** — is the `contact` form listed and is email going to robert@coulsy.co.uk? (60 seconds, check before anything else.)
2. **Experience figure** — 35+ years / since 1989? And what's the correct year on your Advanced Craft certificate (the site says 1988 in one place, 1990 in another)?
3. **VAT number** — give it to me and I'll put it on the contact page properly.
4. **Heritage & bespoke** — rewrite in place (my recommendation: keeps 148 clicks and the rankings, changes who calls) or delete outright (forfeits them)?
5. **Service pruning** — do I merge 18 services down to ~9 and 301 the rest? All 56 towns survive either way.

**If you only do three things this month:** the footer link (1.1), the homepage call button (1.2), and the `.html` redirects (2.1).